using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Moving : MonoBehaviour
{
    //GameObject Ring;
    // float a=Ring.GetComponent<PictureMoving>.vectors;
    //public Vector3 a = PictureMoving.RotationAndPosition();
    // Start is called before the first frame update
    void Start()
    {
        //Debug.Log(a.ToString());
    }

    // Update is called once per frame
    void Update()
    {
       // PictureMoving.RotationAndPosition();
        //gameObject.transform.position = new Vector3(0,a.z, -5/100);
    }
}
